
<div class="section" id="b-section-header" name="Header">
    <div class="widget Header" data-version="2" id="Header1">
        <div class="header image-placement-behind no-image">
            <div class="container">
                <h1><a href="">Twitter-Clone</a></h1>
                <p></p>
            </div>
        </div>
    </div>
</div>
<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
    <a class="navbar-brand" href="<%= home_path %>">Twitter Clone</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
            <a class="nav-link" href="<%= home_path %>">Home
            <span class="sr-only">(current)</span>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="<%= tweets_path %>">My tweets <span class="badge badge-secondary"><%= current_user.tweets.count %></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="<%= follower_users_path %>">Followers <span class="badge badge-secondary"><%= Follow.where(following_user_id: current_user.id).count %></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="<%= following_users_path %>">Following <span class="badge badge-secondary"><%= Follow.where(follower_user_id: current_user.id).count %></span></a>
        </li>
        </ul>
        <div class="ml-auto my-2 my-lg-0">
        <div class="section" id="b-section-navbar-search-form" name="Navbar: search form">
            <div class="widget BlogSearch" data-version="2" id="BlogSearch1">
                <div class="form-inline">
                    <label aria-label="Search this blog" disabled class="form-control mr-sm-2">@<%= current_user&.username %></label>
                    <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-success my-2 my-sm-0", type: "submit" %>
                </div>
            </div>
        </div>
        </div>
    </div>
    </div>
</nav>

